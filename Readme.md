Features

🔐 Firebase Token Verification Middleware
Ensures secure access to protected routes by validating Firebase-issued JWT tokens.

👤 User Management
Stores new users, prevents duplicates, and supports role-based authorization (extensible).

📦 CRUD for Listings (Models)
Add, update, delete, search, filter, and fetch listings with category filtering and latest product support.

💰 Bidding System
Users can place bids, and sellers can view bids securely with token-based access checks.

🔍 Search & Filtering API
Search products by name and filter by category dynamically.